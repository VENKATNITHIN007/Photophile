import { LoginForm } from "@/features/auth/components/LoginForm";

/**
 * Clean production-ready Login page.
 * Logic and layout are encapsulated in the LoginForm component.
 */
export default function LoginPage() {
  return <LoginForm />;
}
